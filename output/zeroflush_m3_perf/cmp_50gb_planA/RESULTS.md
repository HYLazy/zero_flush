# 方案 A（L0 排他所有权）50GB 验证（2026-09-03）

改动：A1 = zeroflush::Open 覆盖 level0 trigger/slowdown/stop=1e8（原生永不消费 L0）；
A2 = materialize l0 融合兜底（L0 字节≥64MB 或文件数≥8 → 无视 ratio 融合）。
参数同窗：16 线程、1KB、sampled/16、base_merge、skip_batching=false、value_cache 64MB。

## 结果

| 配置 | fill ops/s | lvl1/lvl0 | 读命中 |
|---|---|---|---|
| 方案 A 50GB | 20,030 | 1321/961 (42% L0) | 631,918/1M (63.2%) |
| 默认基线 50GB（历史 27,066） | 27,066 | — | 63.3% |
| native 50GB（本批 26,146） | 26,146 | — | 63.3% |
| 方案 A 5GB（对照） | 64,905 (+47%) | 16/254 (94% L0) | 63.2% |

## 结论

证伪（50GB -26%）。5GB 的正收益再次不迁移（同 trigger=32 模式）。机制：
排他生效（l0busy 689→91，L0 不再被原生占用）但 L0 由 ZF 清理时堆积数百文件 →
(a) PlanLocked 每分区持锁全层扫描 L0 文件（O(L0 文件数)）成瓶颈；
(b) force 融合每 8 文件重写整个分区 overlap（50GB 时分区累积大）→ 写放大风暴。
L0 循环无法靠"L0 管理策略"（延迟原生消费 / 排他 + ZF 兜底融合）解决——
两种策略的 50GB 负收益同型。数据完整性全绿（63.2%，零丢失）。
已回滚（回归恢复 35 PASS + 3 既有 FAIL）。
